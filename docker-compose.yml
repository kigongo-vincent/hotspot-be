services:
  database:
    image: postgres:15
    container_name: hotpsotv1

    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres

    ports:
      - "5433:5432"

    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
      